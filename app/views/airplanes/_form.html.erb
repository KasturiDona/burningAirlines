<%= form_for(@airplane) do |f| %>
  <% if @airplane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airplane.errors.count, "error") %> prohibited this airplane from being saved:</h2>

      <ul>
      <% @airplane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_area :name, :required => true, :class=> "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :rows %><br>
    <%= f.number_field :rows, in: 1..5, :class=> "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :columns %><br>
    <%= f.number_field :columns, in: 1..50, :class=> "form-control" %>
  </div>
  <div class="actions form-group">
    <%= f.submit :class => 'submit btn btn-primary'%>
  </div>
<% end %>
