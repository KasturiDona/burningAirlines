<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
	<% @user.reservations.each do |reservation| %>
		<strong>Reservation ID. <%= link_to(reservation.id, reservation) %></strong>
		<strong>Flight Number: <%= reservation.flight.flight_number %></strong>
	<% end %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
